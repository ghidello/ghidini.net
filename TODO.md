# Future Improvements

- **Astro Font API** — Use `fontProviders.local()` or `fontProviders.google()` in `astro.config.mjs` for optimized font loading with `<Font>` component
- **Image optimization** — Use `image()` helper in content collection schema for hero images, enabling Astro's built-in image pipeline (resizing, format conversion, lazy loading)
