# Future Improvements

- **Hero image alt text** — `heroImageAlt` is supported in the blog schema; add real alt text whenever a post sets `heroImage`.
- **About bio** — `src/pages/about.astro` contains a drafted bio; replace it with the exact summary from LinkedIn.
- **Custom OG fonts** — `astro-og-canvas` cards currently use the default font. Wire in Inter/JetBrains Mono via `getImageOptions` for full brand consistency.
